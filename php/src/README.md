# php/src

PHP application and module source. Empty at this task (0391) — the
composer autoload configuration (0392), `Http` value objects (0395-0397),
and `Modules` namespace skeleton (0399+) are added by later Phase B tasks.

Classes here follow the namespace mapping fixed in
`docs/php-namespace-mapping.md`: `MintWiki\<Module>` for module code, with
one directory per module under `src/Modules` once it exists.
